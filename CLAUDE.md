# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is in its initial state. The only tracked file besides `.git` is `README.md`, which currently contains only the project title (`# Poke-App-1`). There is no source code, build system, dependency manifest, test suite, lint configuration, or CI pipeline yet.

As a result, there are no project-specific build, test, or lint commands to document. When the codebase grows, this file should be updated to capture:

- How to install dependencies, build, run, test (including a single test), and lint.
- The high-level architecture and module layout once it spans multiple files.
- Any non-obvious conventions established by maintainers.

Until then, treat any task here as greenfield: confirm the intended language, framework, and project layout with the user before scaffolding, rather than assuming based on the repo name.
